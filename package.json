{
  "name": "myfitnesscoachai",
  "version": "1.0.0",
  "description": "AI-powered personalized fitness app",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "postinstall": "npm run install:all && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["fitness", "ai", "health", "workout"],
  "author": "",
  "license": "ISC"
}
